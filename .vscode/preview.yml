
# .vscode/preview.yml
autoOpen: true # 打开工作空间时是否自动开启所有应用的预览
apps:
  - port: 3000 # 应用的端口
    run: npm start # 应用的启动命令
    root: ./neu_clock # 应用的启动目录
    name: neu_clock # 应用名称
    description: neu_clock。 # 应用描述
    autoOpen: true # 打开工作空间时是否自动开启预览（优先级高于根级 autoOpen）
